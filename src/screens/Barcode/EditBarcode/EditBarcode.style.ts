import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputTypeGroup: {
    flexDirection: "row",
  },
  middleButton: {
    marginHorizontal: 6,
  },
});

export default styles;
