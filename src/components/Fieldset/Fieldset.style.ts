import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  field: {
    borderWidth: 1,
    borderColor: "#101426", //"#1a2138"
    borderStyle: "solid",
    padding: 12,
    borderRadius: 10,
    marginBottom: 16,
  },
});

export default styles;
